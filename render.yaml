services:
  - type: web
    name: daozhu-kart
    runtime: node
    plan: free
    buildCommand: npm install --production
    startCommand: node server/multiplayer-server.js
    envVars:
      - key: NODE_ENV
        value: production
